/**
 * This is the main for the second variant of the cratchit app
 * Also see zeroth:: (CLI user interface with very crude switch-to-state user input processing)
 * Also see first:: (ncurses interface with experimental state machine)
 */
#pragma once

namespace second {
  int main(int argc, char *argv[]);
} // second
