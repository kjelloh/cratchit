# Consider a way to generate (print) a nice looking M journal entry?

I have currently no way to print e.g., a generated M journal entry after a VAT Returns report.

* At least a way to dump into a text file could be enough?
  - Generate M4.txt?
  - Or generate markdown M4.md?
* Maybe a Ux to also select any journal entry and generate a text/md-file?

